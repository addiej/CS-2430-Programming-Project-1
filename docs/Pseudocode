#[Pseudocode and ToDos can be posted in this folder within this section of the repository]

#Mergesort - "Divide and Conquer Algorithm"
#Has O(n * log(n)) running time, which is optimal for comparison-based algorithms
#Mergesort
#Step 1: Split array in half.
#Step 2: Call merge sort on each half to sort them recursively.
#Step 3: Merge both sorted halves into one array.

print("Sorting algorithm options: \nq = Quick Sort \ns = Shaker Sort \nh = Heap Sort \nm = Merge Sort")
UserSortAlgorithmChoice = input("Which of the sorting algorithms would you like to use? ").lower()


if UserSortAlgorithmChoice == 'q':
    print("Quick Sort option chosen.")
    ArrayInputQ = input("Enter in an array of numbers (without commas separating values)")
    ArrayListQ = list(map(int, ArrayInputQ.split()))
    print (f"Array: {ArrayListQ}")
    #TODO implement Quick Sort

if UserSortAlgorithmChoice == 's':
    print("Shaker Sort option chosen.")
    ArrayInputS = input("Enter in an array of numbers (without commas separating values)")
    ArrayListS = list(map(int, ArrayInputS.split()))
    print (f"Array: {ArrayListS}")
    #TODO implement Shaker Sort
    
if UserSortAlgorithmChoice == 'h':
    print("Heap Sort option chosen.")
    ArrayInputH = input("Enter in an array of numbers (without commas separating values)")
    ArrayListH = list(map(int, ArrayInputH.split()))
    print (f"Array: {ArrayListH}")
    #TODO implement Heap Sort

if UserSortAlgorithmChoice == 'm':
    print("Heap Merge Sort option chosen.")
    ArrayInputM = input("Enter in an array of numbers (without commas separating values)")
    ArrayListM = list(map(int, ArrayInputM.split()))
    print (f"Array: {ArrayListM}")
    #TODO implement merge Sort



#INTELLECTUAL CREDITS:
print ("INTELLECTUAL CREDITS: \n\n")
IntellectualCreditsMergeSort1 = "Mergesort implementation methods and logic provided by FelixTechTips on YouTube"
IntellectualCreditsMergeSort1URL = "https://www.youtube.com/watch?v=cVZMah9kEjI&t=544s"
IntellualCreditsMergeSortLogic1 = "Logic for list(map(int, x.split())) method call from:"
IntellualCreditsMergeSortLogic1URL = "https://stackoverflow.com/questions/19555472/change-a-string-of-integers-separated-by-spaces-to-a-list-of-int"

print (IntellectualCreditsMergeSort1)
print (f"URL: {IntellectualCreditsMergeSort1URL}")
print (IntellualCreditsMergeSortLogic1)
print (f"URL: {IntellualCreditsMergeSortLogic1URL}")
